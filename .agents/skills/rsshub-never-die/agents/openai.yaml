interface:
  display_name: "Rsshub Never Die"
  short_description: "Repo-specific patterns and workflows for rsshub-never-die"
  default_prompt: "Use the rsshub-never-die repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true